import React from 'react';

import { Container, Title } from './styled';

export default function Loading() {
    return (
        <Container>
            <Title>Loading</Title>
        </Container>
    );
}